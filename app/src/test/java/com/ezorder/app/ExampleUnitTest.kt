package com.ezorder.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder to confirm the JVM unit test source set is wired up.
 * Real unit tests (repository logic, order/reservation state machines,
 * cart math) start arriving from Phase 2 onward.
 */
class ExampleUnitTest {
    @Test
    fun additionIsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
