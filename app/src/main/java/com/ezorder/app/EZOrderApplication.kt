package com.ezorder.app

import android.app.Application

/**
 * EZORDER application entry point.
 *
 * Intentionally empty in Phase 0. This is where a dependency-injection
 * container or manual service locator for the mock repositories
 * (MockRestaurantRepository, MockMenuRepository, MockOrderRepository,
 * MockReservationRepository — see spec section 64) will be wired up in
 * Phase 2, once those repositories exist.
 */
class EZOrderApplication : Application()
