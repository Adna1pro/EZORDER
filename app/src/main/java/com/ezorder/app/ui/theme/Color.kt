package com.ezorder.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Placeholder brand palette for Phase 0.
 *
 * This is intentionally minimal — just enough for MaterialTheme to have
 * non-default colors so the app doesn't look like generic Material
 * boilerplate. The full EZORDERColors token set (spec section 57) —
 * semantic colors for order states, table availability, ratings, etc. —
 * is designed and built in Phase 1 (Design System), not here.
 */

// Warm, food-focused primary — a terracotta/spice tone rather than a
// generic Material blue/purple.
val EzOrderPrimary = Color(0xFFE9542A)
val EzOrderPrimaryDark = Color(0xFFB53C18)
val EzOrderOnPrimary = Color(0xFFFFFFFF)

val EzOrderSecondary = Color(0xFF2A6B4F)
val EzOrderOnSecondary = Color(0xFFFFFFFF)

val EzOrderBackground = Color(0xFFFFFBF7)
val EzOrderSurface = Color(0xFFFFFFFF)
val EzOrderOnBackground = Color(0xFF231A16)
val EzOrderOnSurface = Color(0xFF231A16)

val EzOrderError = Color(0xFFBA1A1A)
val EzOrderOnError = Color(0xFFFFFFFF)
